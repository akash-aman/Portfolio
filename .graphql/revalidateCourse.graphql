query revalidateCourse($slug: String!) {
	course(slug: $slug) {
		data {
			attributes {
				chapters {
					chapter {
						data {
							attributes {
								Slug
							}
						}
					}
				}
			}
		}
	}
}
